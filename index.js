$(function() {

    // var board = new Board(4);
    var controller = new tabController($('.control_active'));

    // controller.domElem.on({
    //     enter: fn(),
    //     out: fn()
    // })

});
